name: MyBot AvalonBot freerozizi

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  build-and-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'

      - name: Install Go dependencies
        run: |
          go mod init bot 2>/dev/null || true
          go get github.com/sandertv/go-raknet 2>/dev/null || true
          go get github.com/sirupsen/logrus 2>/dev/null || true

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install discord.py

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential gcc g++ make libpcap-dev libssl-dev wget

      - name: Build binaries
        run: |
          echo "=== COMPILANDO BINARIOS ==="

          # Compilar métodos principales
          binaries=(
            "hex.c:hex"
            "udp.go:udp" #Compilando el metodo GO udp.go
            "ovhudp.c:ovhudp"
            "ovhtcp.c:ovhtcp"
            "udppps.c:udppps"
            "tcp.c:tcp" # Agregando el compilar tcp
            "ovhbypass.c:ovhbypass" # Pene
          )

          for entry in "${binaries[@]}"; do
            IFS=':' read -r source binary libs <<< "$entry"

            if [ -f "$source" ]; then
              echo -n "🔨 $source → $binary ... "

              if [[ "$source" == *.go ]]; then #Detectando si el archivo es .go
                 go build -o "$binary" "$source" 2>/dev/null && { #Compilando GO
                   chmod +x "$binary"
                   echo "✅"
                 } || echo "⚠️"
              else
                if [ -n "$libs" ]; then
                  gcc -O3 -pthread "$source" -o "$binary" $libs 2>/dev/null && {
                    chmod +x "$binary"
                    echo "✅"
                  } || echo "⚠️"
                else
                  gcc -O3 "$source" -o "$binary" 2>/dev/null && {
                    chmod +x "$binary"
                    echo "✅"
                  } || echo "⚠️"
                fi
              fi
            fi
          done

          echo ""
          echo "📂 Ejecutables disponibles:"
          ls -la | grep -E '^-..x.*' | awk '{print $9}' || echo "No hay ejecutables"

      - name: 🚀 EJECUTAR BOT CON KEYS
        env:
          KEYS: ${{ secrets.KEYS }}  # ¡IMPORTANTE! Esto pasa KEYS al bot.py
        run: |
          echo "========================================"
          echo "🤖 INICIANDO BOT UDP/TCP/AMP/L7"
          echo "========================================"

          # DEBUG: Verificar que KEYS se recibió
          echo "🔍 VERIFICANDO CONFIGURACIÓN:"
          echo "   - Directorio: $(pwd)"
          echo "   - Archivo bot.py: $(ls -la bot.py 2>/dev/null && echo "✅" || echo "❌")"
          echo "   - KEYS recibido: $([ -n "$KEYS" ] && echo "✅ (${#KEYS} chars)" || echo "❌ VACÍO")"

          if [ -z "$KEYS" ]; then
            echo ""
            echo "❌❌❌ ERROR: KEYS está VACÍO ❌❌❌"
            echo ""
            echo "SOLUCIÓN INMEDIATA:"
            echo "1. El secret 'KEYS' NO está configurado o está vacío"
            echo "2. Ve a: Settings → Secrets and variables → Actions"
            echo "3. Haz clic en 'KEYS' para verificar su contenido"
            echo "4. Si está vacío, edítalo y pega tu token"
            echo ""
            echo "Token de Discord debe tener ~59 caracteres como:"
            echo "pito"
            exit 1
          fi

          # Verificar formato del token
          if [ ${#KEYS} -lt 50 ]; then
            echo "⚠️  ADVERTENCIA: Token muy corto (${#KEYS} chars)"
            echo "   Debe tener ~59 caracteres mínimo"
          fi

          echo ""
          echo "✅ CONFIGURACIÓN CORRECTA"
          echo "   Token recibido: ${#KEYS} caracteres"
          echo "   Vista previa: ${KEYS:0:15}..."
          echo ""
          echo "🎮 MÉTODOS DISPONIBLES:"

          # Mostrar métodos según ejecutables
          for method in hex udp ovhbypass ovhudp ovhtcp udppps tcp; do
            [ -f "$method" ] && echo "   • $method"
          done

          echo ""
          echo "🚀 INICIANDO bot.py..."
          echo "========================================"

          # Ejecutar bot.py (KEYS ya está como variable de entorno)
          python3 bot.py

          echo ""
          echo "========================================"
          echo "🤖 BOT FINALIZADO"
          echo "========================================"
